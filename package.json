{
  "name": "beroepenavond",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:apply:local": "for f in schema/*.sql; do echo \"-> $f\"; wrangler d1 execute beroepenavond --local --file=\"$f\"; done",
    "db:apply:remote": "for f in schema/*.sql; do echo \"-> $f\"; wrangler d1 execute beroepenavond --remote --file=\"$f\"; done"
  },
  "dependencies": {
    "hono": "^4.12.34",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^6.0.3",
    "wrangler": "^4.116.0"
  }
}
