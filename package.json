{
  "name": "beroepenavond",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply:local": "wrangler d1 execute beroepenavond --local --file=schema/001_init.sql && wrangler d1 execute beroepenavond --local --file=schema/002_seed.sql",
    "db:apply:remote": "wrangler d1 execute beroepenavond --remote --file=schema/001_init.sql && wrangler d1 execute beroepenavond --remote --file=schema/002_seed.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250612.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
